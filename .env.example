# MindCare — variables d'environnement (copier en .env puis renseigner)
# Utilisé par docker-compose et par l'application.

# Supabase — projet PostgreSQL + Auth (la sécurité repose sur la RLS, pas sur le secret de la clé)
NEXT_PUBLIC_SUPABASE_URL=https://votre-projet.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Groq — clé d'API pour l'IA Luna (serveur uniquement, jamais exposée au navigateur)
GROQ_API_KEY=gsk_...
